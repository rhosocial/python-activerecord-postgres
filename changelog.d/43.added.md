Added production-style PostgreSQL time partition operation tests covering microsecond `TIMESTAMP(6)` boundaries, future partition pre-creation, continuous partition pruning, index usage, metadata changes, and cold archival via `DETACH PARTITION`.