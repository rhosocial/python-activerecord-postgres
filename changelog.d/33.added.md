Added schema namespace support for PostgreSQL backend, including get_default_schema() method, schema-aware column formatting with alias handling, and support for multiple PostgreSQL extensions (PostGIS, pgvector, UUID, hstore).
