# schema tests

SchemaSupport capability of the PostgreSQL dialect (database -> schema -> table namespace) and schema-aware DDL formatting.

## Key files

- `test_schema_support.py` — schema capability and DDL formatting
