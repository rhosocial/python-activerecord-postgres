# dml tests

PostgreSQL DML integration: real-database CRUD, column type mapping and execute_many batch operations for sync and async backends.

## Key files

- `test_column_mapping_backend.py` — column type mapping
- `test_crud_backend.py` — CRUD integration
- `test_execute_many.py` — batch execution
