# protocol tests

Protocol conformance of the PostgreSQL dialect/backend against the core ActiveRecord protocol surface, and protocol non-overlap guarantees.

## Key files

- `test_protocol_conformance.py` — PostgresDialect protocol conformance and non-overlap
